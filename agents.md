# Agent Guidelines

- Update `changelog.md` for every feature added.
- Use conventional commits (`feat:`, `fix:`, `chore:`, `docs:`, `refactor:`).
- Run `pnpm run verify` before committing.
- Never commit secrets or credentials.
